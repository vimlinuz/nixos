{
  programs.vesktop = {
    enable = true;
    vencord = {
      settings = {
        useSystem = true;

        plugins = {
          Translate = {
            enable = true;
          };

          FakeNitro = {
            enable = true;
          };
        };
      };

    };
  };
}
